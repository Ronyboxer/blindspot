//
//  Secrets.example.swift
//  Blind Spot
//
//  TEMPLATE for the git-ignored Secrets.swift. Copy this file to `Secrets.swift`
//  (same folder) and fill in your values, then run `xcodegen generate`.
//
//  Find the values in Supabase → Project Settings → API:
//    • supabaseURL     = "Project URL"
//    • supabaseAnonKey = "anon / public" key   (NOT the service_role key)
//
//  NOTE: keep this `.example.swift` excluded from the build target — it only
//  exists as documentation. (XcodeGen excludes *.example.swift via project.yml.)
//

import Foundation

// enum Secrets {
//     static let supabaseURL = "https://YOUR-PROJECT-ref.supabase.co"
//     static let supabaseAnonKey = "YOUR-ANON-PUBLIC-KEY"
// }
